

/* label - texto que aparece en el textfield (como placeholder en html),
	cuando esta en focus esta se coloca en arriba (como el label en html).
	*/
	TextField(
		decoration: InputDecoration(
			labelText: this.label,
			),	
		);

/* 

*/
TextField(
	keyboardType: TextInputType.number,
)